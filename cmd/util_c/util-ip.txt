This command will connect to https://ipecho.net/plain to retrieve the public IP address of the machine as seen by the internet

The output will be the public IP address of the machine. For a more verbose output, run with flag -v